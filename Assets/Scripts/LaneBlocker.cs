using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LaneBlocker : MonoBehaviour
{
    GameObject[] rocks;

    // Start is called before the first frame update
    void Start()
    {
        SpawnRocksOnLane();
    }

    // Create a mechanism to spawn a random rock from array on each block of the lane
    private void SpawnRocksOnLane()
    { 

    }
}
